export type Deal = {
  id: string;
  title: string;
  category: string;
  platform: string;
  description: string;
  badge: string;
  priceNote: string;
  affiliateUrl: string;
};
export type Category = { name: string; description: string; count: string; };
export type Feature = { title: string; description: string; };
